# Задание 3.2 — Именованные каналы FIFO

## Условие
**Вариант 2**: Через FIFO передать 10 целых чисел. Получатель вычисляет среднее арифметическое.

## Требования
- Использовать `mkfifo()`, `open()`, `read()`, `write()`, `close()`, `unlink()`
- Обрабатывать ошибки через `perror()`
- Компиляция: `gcc -Wall -Wextra -o task task.c`

## Компиляция и запуск
```bash
gcc -Wall -Wextra -o task task.c
./task