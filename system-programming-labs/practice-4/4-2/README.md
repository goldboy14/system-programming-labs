# Задание 4.2 — Отображение файлов в память (mmap)

## Условие
**Вариант 2**: Отобразить файл с числами и найти максимальное значение.

## Требования
- Использовать `open()`, `fstat()`, `mmap()`, `munmap()`, `close()`
- Обрабатывать ошибки через `perror()`
- Компиляция: `gcc -Wall -Wextra -o task task.c`

## Компиляция и запуск
```bash
gcc -Wall -Wextra -o task task.c
./task