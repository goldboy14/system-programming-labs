# Задание 4.1 — Динамическая память

## Условие
**Вариант 2**: Выделить память под строку, считать строку с клавиатуры, определить её длину.

## Требования
- Использовать `malloc()`, `calloc()`, `realloc()`, `free()`
- Проверять результат выделения памяти
- Компиляция: `gcc -Wall -Wextra -o task task.c`

## Компиляция и запуск
```bash
gcc -Wall -Wextra -o task task.c
./task